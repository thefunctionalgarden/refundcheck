%% -*- mode: erlang -*-
[
    {kernel, [
        {logger, [
            {
                handler, 
                default, 
                logger_std_h,
                #{
                    level => info,
                    config => #{
                        file => "log/info.log"
                    }
                }
            }
        ]}
    ]},

    {refundcheck, [
        {db_host, "localhost"},
        {db_user, "cam"},
        {db_pass, "Casteril0!"},
        {http_port, "2020"}
    ]}
].

